package model;

public class staircase extends entity {

    directions dir;

    staircase(int setHealth, int setSpeed, int setDamage, int setId) {
        super(setHealth, setSpeed, setDamage, setId);
    }
    
}
